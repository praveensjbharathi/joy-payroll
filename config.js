// This file contains only public, browser-safe Supabase connection settings.
// Edit it in cPanel File Manager after uploading the built website.
// NEVER put a secret key, service-role key, database password, or access token here.
window.JOY_PAYROLL_CONFIG = {
  supabaseUrl: "https://YOUR_PROJECT_REF.supabase.co",
  supabasePublishableKey: "YOUR_SUPABASE_PUBLISHABLE_KEY",
  // Leave apiUrl empty unless the payroll Edge Function uses a different URL.
  apiUrl: "",
};
