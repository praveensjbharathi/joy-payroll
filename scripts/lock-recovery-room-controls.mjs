import { readFile, writeFile } from "node:fs/promises";
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";
const root = dirname(dirname(fileURLToPath(import.meta.url)));
const appPath = join(root, "app/reports-recovery.tsx");
let app = await readFile(appPath, "utf8");
app = app.replace(/\.filter\(\(row\) => row\.charge \|\| row\.dated\.length \|\| row\.shared > 0\);/g,".filter((row) => Boolean(row.item) || Boolean(row.charge) || row.dated.length > 0 || row.shared > 0);");
app = app.replace('className="room-print-toolbar"','className="room-print-toolbar recovery-left-room-toolbar"');
await writeFile(appPath, app, "utf8");
const livePath = join(root, "supabase-frontend/live-enhancements.ts");
let live = await readFile(livePath, "utf8");
live = live.replace(/\n\/\/ JOY_RECOVERY_FINALIZER_V3[\s\S]*?\/\/ END_JOY_RECOVERY_FINALIZER_V3\n?/g, "\n");
live += `
// JOY_RECOVERY_FINALIZER_V3
function joyRecoveryFinalCleanupV3(){document.querySelectorAll<HTMLElement>(".panel").forEach((panel)=>{const h2=panel.querySelector("h2");if(!h2?.textContent?.includes("final payable"))return;const toolbars=Array.from(panel.querySelectorAll<HTMLElement>(".room-print-toolbar"));const keep=toolbars.find((t)=>t.classList.contains("recovery-left-room-toolbar"))??toolbars[0]??null;toolbars.forEach((t)=>{if(t!==keep)t.remove();});panel.querySelectorAll<HTMLSelectElement>("select").forEach((s)=>{if(keep?.contains(s))return;const roomish=(s.getAttribute("aria-label")||"").toLowerCase().includes("room")||Array.from(s.options).some((o)=>(o.textContent||"").toLowerCase().includes("select room"));if(roomish)s.remove();});panel.querySelectorAll<HTMLButtonElement>("button").forEach((b)=>{if(keep?.contains(b))return;const t=(b.textContent||"").toLowerCase();if((t.includes("selected room")||t.includes("all room"))&&t.includes("a4")&&t.includes("landscape"))b.remove();if(t.includes("bulk recovery slip")&&t.includes("a4")&&t.includes("portrait"))b.remove();if(t.includes("all room")&&t.includes("a4")&&t.includes("portrait"))b.remove();});});}
function joyRecoveryPrintAllRoomsV3(){const panel=Array.from(document.querySelectorAll<HTMLElement>(".panel")).find((p)=>p.querySelector("h2")?.textContent?.includes("final payable"));if(!panel)return false;const toolbar=panel.querySelector<HTMLElement>(".recovery-left-room-toolbar")??panel.querySelector<HTMLElement>(".room-print-toolbar");const allButton=Array.from(toolbar?.querySelectorAll<HTMLButtonElement>("button")??[]).find((b)=>{const t=(b.textContent||"").toLowerCase();return t.includes("all room")&&t.includes("a4")&&t.includes("landscape");});if(!allButton||allButton.dataset.joyAllRoomsV3==="1")return false;allButton.dataset.joyAllRoomsV3="1";allButton.addEventListener("click",(event)=>{event.preventDefault();event.stopImmediatePropagation();const employeeTable=panel.querySelector<HTMLTableElement>("table");if(!employeeTable)return;const headers=Array.from(employeeTable.tHead?.rows[0]?.cells??[]).map((c)=>c.textContent?.trim()??"");const roomIndex=headers.findIndex((h)=>h==="Room");const voucherIndex=headers.findIndex((h)=>h==="Voucher");if(roomIndex<0)return;const select=toolbar?.querySelector<HTMLSelectElement>("select")??null;const printOverall=()=>{const rows=Array.from(employeeTable.tBodies[0]?.rows??[]).map((r)=>r.cloneNode(true) as HTMLTableRowElement);joyBuildAllRoomSheetsV3(employeeTable,rows,roomIndex,voucherIndex);};if(select?.value){const old=select.value;select.value="";select.dispatchEvent(new Event("change",{bubbles:true}));requestAnimationFrame(()=>requestAnimationFrame(()=>{printOverall();select.value=old;select.dispatchEvent(new Event("change",{bubbles:true}));}));}else printOverall();},true);return true;}
function joyBuildAllRoomSheetsV3(employeeTable:HTMLTableElement,sourceRows:HTMLTableRowElement[],roomIndex:number,voucherIndex:number){document.querySelector(".room-recovery-print-layer")?.remove();const sharedPanel=Array.from(document.querySelectorAll<HTMLElement>(".panel")).find((p)=>{const t=p.querySelector("h2")?.textContent||"";return t.includes("Gas, Ration")&&t.includes("Provision");});const sharedTable=sharedPanel?.querySelector<HTMLTableElement>("table")??null;const sharedHeaders=Array.from(sharedTable?.tHead?.rows[0]?.cells??[]).map((c)=>c.textContent?.trim()??"");const sharedRoomIndex=sharedHeaders.findIndex((h)=>h==="Room");const rooms=[...new Set([...sourceRows.map((r)=>r.cells[roomIndex]?.textContent?.trim()??"").filter((r)=>r&&r!=="—"),...Array.from(sharedTable?.tBodies[0]?.rows??[]).map((r)=>sharedRoomIndex>=0?r.cells[sharedRoomIndex]?.textContent?.trim()??"":"").filter((r)=>r&&r!=="—")])].sort((a,b)=>a.localeCompare(b,undefined,{numeric:true}));const layer=document.createElement("div");layer.className="room-recovery-print-layer";rooms.forEach((roomName)=>{const employeeRoomRows=sourceRows.filter((r)=>r.cells[roomIndex]?.textContent?.trim()===roomName);const sharedRow=sharedTable&&sharedRoomIndex>=0?Array.from(sharedTable.tBodies[0]?.rows??[]).find((r)=>r.cells[sharedRoomIndex]?.textContent?.trim()===roomName):undefined;if(!employeeRoomRows.length&&!sharedRow)return;const sheet=document.createElement("section");sheet.className="room-recovery-print-sheet";const h1=document.createElement("h1");h1.textContent="FINALIZED ROOM-WISE SALARY RECOVERY STATEMENT";const h2=document.createElement("h2");h2.textContent="Room: "+roomName;sheet.append(h1,h2);if(sharedRow){const summary=document.createElement("div");summary.className="room-recovery-summary";["Gas","Ration","Provision","Occupants"].forEach((label)=>{const i=sharedHeaders.findIndex((h)=>h===label);const box=document.createElement("div");box.innerHTML="<span>"+(label==="Occupants"?"Roommates":label)+"</span><strong>"+(i>=0?sharedRow.cells[i]?.textContent?.trim()||"—":"—")+"</strong>";summary.appendChild(box);});sheet.appendChild(summary);}if(employeeRoomRows.length){const clone=employeeTable.cloneNode(true) as HTMLTableElement;const body=clone.tBodies[0];if(body)body.innerHTML="";employeeRoomRows.forEach((source)=>{const row=source.cloneNode(true) as HTMLTableRowElement;if(voucherIndex>=0&&row.cells[voucherIndex])row.deleteCell(voucherIndex);body?.appendChild(row);});const head=clone.tHead?.rows[0];if(head&&voucherIndex>=0&&head.cells[voucherIndex])head.deleteCell(voucherIndex);sheet.appendChild(clone);}layer.appendChild(sheet);});if(!layer.children.length)return;document.body.appendChild(layer);const style=document.createElement("style");style.textContent="@page{size:A4 landscape;margin:7mm;}@media print{body>*:not(.room-recovery-print-layer){display:none!important}.room-recovery-print-layer{display:block!important}.room-recovery-print-sheet{display:block!important;break-before:auto!important;break-after:page!important;page-break-before:auto!important;page-break-after:always!important;min-height:0!important;height:auto!important;margin:0!important;padding:0!important;overflow:visible!important}.room-recovery-print-sheet:last-child{break-after:auto!important;page-break-after:auto!important}.room-recovery-print-sheet:empty{display:none!important}.room-recovery-print-layer::before,.room-recovery-print-layer::after{display:none!important;content:none!important}}";document.head.appendChild(style);const cleanup=()=>{layer.remove();style.remove();window.removeEventListener("afterprint",cleanup);};window.addEventListener("afterprint",cleanup);window.print();setTimeout(cleanup,3000);}
const joyRecoveryFinalObserverV3=new MutationObserver(()=>{joyRecoveryFinalCleanupV3();joyRecoveryPrintAllRoomsV3();});joyRecoveryFinalObserverV3.observe(document.documentElement,{childList:true,subtree:true});joyRecoveryFinalCleanupV3();joyRecoveryPrintAllRoomsV3();
// END_JOY_RECOVERY_FINALIZER_V3
`;
await writeFile(livePath, live, "utf8");
console.log("Recovery V3: all-room employee print, duplicate control cleanup, and blank print page removal applied.");
